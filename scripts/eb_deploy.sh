#!/bin/bash

# Script to automate deployment to different elastic beanstalk environments

# Named Arguments---------------------------------------------------
# -p : Root directory of the django project, defaults to './'
